# CommandMenu

Command palette (Cmd+K) with search and keyboard nav

**Category:** overlay

## Import

```tsx
import { CommandMenu, type CommandMenuProps, type CommandItem } from 'jind-ui-kit';
```

## Props

| Prop | Type |
|------|------|
| `open` | `boolean` |
| `onOpenChange` | `(open: boolean) => void` |
| `items` | `CommandItem[]` |
| `placeholder` | `string` |
| `emptyMessage` | `string` |
| `footer` | `ReactNode` |

