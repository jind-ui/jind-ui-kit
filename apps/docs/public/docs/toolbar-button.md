# ToolbarButton

Button styled for toolbar use

**Category:** utility

## Import

```tsx
import { ToolbarButton } from 'jind-ui-kit';
import type { ToolbarButtonProps } from 'jind-ui-kit';
```

## Props

| Prop | Type |
|------|------|
| `name` | `string` |
| `active` | `boolean` |
| `disabled` | `boolean` |
| `label` | `string` |
| `accent` | `string` |
| `onClick` | `React.MouseEventHandler<HTMLButtonElement>` |

