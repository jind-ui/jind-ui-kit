# Accordion

Expandable/collapsible content sections

**Category:** navigation

## Import

```tsx
import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from 'jind-ui-kit';
import type { AccordionProps, AccordionItemProps, AccordionTriggerProps, AccordionContentProps } from 'jind-ui-kit';
```

## Props

| Prop | Type |
|------|------|
| `value` | `string[]` |
| `defaultValue` | `string[]` |
| `onChange` | `(value: string[]) => void` |
| `multiple` | `boolean` |
| `children` | `ReactNode` |

